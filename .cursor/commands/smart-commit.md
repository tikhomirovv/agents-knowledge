# Smart Commit

**Описание:** Анализирует незакоммиченные изменения в репозитории и генерирует структурированное сообщение коммита на английском языке.

**Активация:** Используй эту команду, когда пользователь просит создать коммит, сгенерировать сообщение для коммита или сделать "smart commit".

## Когда использовать

Используй эту команду, когда:
- Пользователь просит создать коммит с автоматическим сообщением
- Нужно сгенерировать описание изменений для коммита
- Пользователь говорит "smart commit", "commit changes", "создай коммит" и т.д.

## Процесс выполнения

### Шаг 1: Проверка статуса Git

1. **Проверь, что это Git репозиторий:**
   - Если нет `.git` папки, сообщи пользователю, что это не Git репозиторий

2. **Получи список измененных файлов:**
   - Используй `run_terminal_cmd` для выполнения `git status --short` или `git status`
   - Определи, какие файлы изменены, добавлены, удалены или переименованы
   - Если нет изменений, сообщи пользователю

### Шаг 2: Анализ изменений

1. **Для каждого измененного файла:**
   - Используй `run_terminal_cmd` для выполнения `git diff <file>` для измененных файлов
   - Для новых файлов используй `read_file` чтобы понять содержимое
   - Для удаленных файлов используй `git diff --cached` или `git log` чтобы понять, что было удалено

2. **Проанализируй изменения:**
   - Определи тип изменений (feature, fix, refactor, docs, style, test, chore)
   - Пойми суть изменений в каждом файле
   - Выдели ключевые изменения

### Шаг 3: Генерация сообщения коммита

1. **Определи общий тип изменений:**
   - `feat:` - новая функциональность
   - `fix:` - исправление бага
   - `refactor:` - рефакторинг кода
   - `docs:` - изменения в документации
   - `style:` - форматирование, отсутствующие точки с запятой и т.д.
   - `test:` - добавление или изменение тестов
   - `chore:` - обновление задач сборки, настройки и т.д.

2. **Создай краткое описание (subject line):**
   - Одно предложение (до 50-72 символов)
   - Начинается с заглавной буквы
   - Без точки в конце
   - Описывает что было сделано, а не почему

3. **Создай детальный список изменений (body):**
   - Перечисли основные изменения по файлам или функциональности
   - Используй маркированный список
   - Каждый пункт - одно предложение
   - Группируй связанные изменения
   - Ограничься 5-10 пунктами для читаемости

### Шаг 4: Форматирование сообщения

**Формат сообщения коммита:**

```
<type>: <short description>

<detailed list of changes>
```

**Примеры:**

```
feat: Add smart commit command for automatic commit messages

- Add smart-commit.md command to .cursor/commands/
- Implement git status and diff analysis
- Generate structured commit messages with type, description, and detailed changes
- Support multiple change types (feat, fix, refactor, docs, etc.)
```

```
fix: Update marketer role.md with new attention_traps knowledge

- Add attention_traps.md to knowledge base index
- Update knowledge list in marketer role.md
- Maintain alphabetical order in knowledge sections
```

```
refactor: Reorganize knowledge management system structure

- Move knowledge templates to centralized location
- Update command references to new template paths
- Improve file organization for better maintainability
```

### Шаг 5: Предложение коммита

1. **Покажи сгенерированное сообщение пользователю:**
   - Отформатируй красиво с использованием markdown
   - Спроси подтверждение перед созданием коммита

2. **Если пользователь подтверждает:**
   - Выполни `git add .` или `git add <specific files>`
   - Выполни `git commit -m "<subject>" -m "<body>"`
   - Покажи результат

3. **Если пользователь хочет изменить:**
   - Предложи отредактировать сообщение
   - Учти пожелания пользователя

## Правила генерации сообщений

### Типы изменений (Conventional Commits):

- **feat:** - Новая функциональность для пользователя
- **fix:** - Исправление бага
- **refactor:** - Изменение кода без изменения функциональности
- **docs:** - Только изменения в документации
- **style:** - Изменения, которые не влияют на смысл кода (пробелы, форматирование)
- **test:** - Добавление или исправление тестов
- **chore:** - Изменения в процессе сборки или вспомогательных инструментах

### Правила написания:

1. **Subject line:**
   - Используй императив ("Add" вместо "Added" или "Adds")
   - Не более 50-72 символов
   - Начинай с заглавной буквы
   - Без точки в конце

2. **Body:**
   - Объясняй что и почему изменено, а не как
   - Используй маркированный список
   - Группируй связанные изменения
   - Ограничься 5-10 пунктами

3. **Общие правила:**
   - Пиши на английском языке
   - Будь конкретным и информативным
   - Избегай общих фраз типа "update files" или "fix bugs"
   - Указывай конкретные файлы или функции, если это важно

## Примеры использования

**Пример 1: Добавление новой функциональности**
```
Изменения:
- .cursor/commands/smart-commit.md (новый файл)
- README.md (обновлен)

Сообщение:
feat: Add smart commit command for automatic commit message generation

- Create smart-commit.md command in .cursor/commands/
- Implement git status and diff analysis functionality
- Add commit message generation with conventional commits format
- Update README with command usage instructions
```

**Пример 2: Исправление и обновление**
```
Изменения:
- agents/marketer/role.md (изменен)
- agents/marketer/knowledge/attention_traps.md (новый файл)

Сообщение:
feat: Add attention traps knowledge to marketer agent

- Add attention_traps.md knowledge file based on Ben Parr's book
- Update marketer role.md to include new knowledge in index
- Structure knowledge with seven attention tools and three attention types
```

**Пример 3: Рефакторинг**
```
Изменения:
- management/templates/knowledge.md (изменен)
- management/instructions/add_knowledge.md (изменен)

Сообщение:
refactor: Improve knowledge template structure and instructions

- Simplify knowledge template format for better readability
- Update add_knowledge.md with clearer step-by-step instructions
- Add more examples and best practices to documentation
```

## Важные замечания

- **Всегда анализируй реальные изменения**, не придумывай
- **Группируй связанные изменения** в одном коммите
- **Не создавай коммит автоматически** без подтверждения пользователя
- **Используй правильный тип** изменений согласно Conventional Commits
- **Будь конкретным** в описании изменений
- **Ограничь длину** сообщения для читаемости

## Формат ответа пользователю

После анализа изменений покажи:

```
📝 Сгенерированное сообщение коммита:

<type>: <subject>

<detailed changes>

---
Создать коммит с этим сообщением? (y/n)
```

После создания коммита:

```
✅ Коммит успешно создан!

📦 Commit: <hash>
💬 Message: <message>

Изменения закоммичены.
```

---

*Эта команда помогает создавать информативные и структурированные сообщения коммитов согласно стандарту Conventional Commits.*
